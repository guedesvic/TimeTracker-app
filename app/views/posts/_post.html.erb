<tr>
  <th>
    <%= post.id %>
  </th>
  <th>
    <%= "#{post.user.full_name}"%>
  </th>
  <th>
    <%= post.date %>
  </th>
  <th>
    <%= truncate(post.rationale) %>
  </th>
  <th>
    <%= link_to 'Edit', edit_post_path(post), id: "edit_#{post.id}" %>
  </th>
  <th>
    <%= link_to 'Delete', post_path(post), method: :delete, data: { confirm: "Are you sure?" }, id: "delete_post_#{post.id}_from_index" %>
  </th>
</tr>
</tr>